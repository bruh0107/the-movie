import { ProfileDetail } from "@/widgets/profile";

const ProfilePage = () => {
    return (
        <div className="py-30">
            <ProfileDetail />
        </div>
    );
};

export default ProfilePage;