import { MovieCatalog } from "@/widgets/movie";

const CatalogPage = () => {

    return (
        <div className="main-container">
            <MovieCatalog />
        </div>
    );
};

export default CatalogPage;